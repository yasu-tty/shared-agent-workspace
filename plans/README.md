# Plans Directory

- `template.md` をコピーして案件別 plan を作る
- 命名は `YYYY-MM-short-name.md` を推奨
- 現在状態は各 plan 冒頭付近の `## Status` を正本とする
- Progress log は履歴であり、現在状態の代替にしない
- 実装中に発見があれば plan を更新する
- 複数 agent が協調した場合も progress log を残す
- 完了後は `Result`, `Completed`, `Remaining work` を更新する
